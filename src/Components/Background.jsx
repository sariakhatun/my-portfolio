import React, { useState } from 'react';

const Background = () => {
    let [stars,setStars]=useState();
    return (
        <div>
            <h1>background</h1>
        </div>
    );
};

export default Background;